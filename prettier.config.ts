import { type Config } from 'prettier';

const config: Config = {
  printWidth: 120,
  semi: true,
  singleQuote: true,
  trailingComma: 'none',
  tabWidth: 2,
  useTabs: false
};

export default config;
